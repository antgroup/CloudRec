name: Secret Scan

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - v*
      - lite-v*

permissions:
  contents: read
  pull-requests: read

jobs:
  gitleaks:
    name: Gitleaks
    runs-on: ubuntu-latest
    env:
      GITLEAKS_VERSION: 8.30.1
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Gitleaks CLI
        run: |
          set -euo pipefail
          archive="gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz"
          base_url="https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}"
          curl -fsSLo "${RUNNER_TEMP}/${archive}" "${base_url}/${archive}"
          curl -fsSLo "${RUNNER_TEMP}/gitleaks_checksums.txt" "${base_url}/gitleaks_${GITLEAKS_VERSION}_checksums.txt"
          (cd "${RUNNER_TEMP}" && grep " ${archive}$" gitleaks_checksums.txt | sha256sum -c -)
          tar -xzf "${RUNNER_TEMP}/${archive}" -C "${RUNNER_TEMP}" gitleaks
          sudo install -m 0755 "${RUNNER_TEMP}/gitleaks" /usr/local/bin/gitleaks
          gitleaks version
      - name: Run Gitleaks
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            gitleaks git . --config .gitleaks.toml --redact=100 --verbose --log-opts="origin/${{ github.base_ref }}..HEAD"
          else
            gitleaks git . --config .gitleaks.toml --redact=100 --verbose
          fi
